Generate 5 flashcards for the topic "{{topic}}" for Class 5-6.
Each card must have a "front" (question or term) and a "back" (answer or definition).
Format strictly as JSON array:
[{"front": "...", "back": "..."}]
