{
  "name": "gurukul-ai-root",
  "version": "1.0.0",
  "description": "Gurukul AI - Multi-platform Learning System",
  "private": true,
  "scripts": {
    "frontend:dev": "npm --prefix frontend-nextjs run dev",
    "frontend:build": "npm --prefix frontend-nextjs run build",
    "backend:dev": "cd backend && python -m src.main",
    "desktop:dev": "npm --prefix frontend-nextjs run desktop:dev",
    "desktop:build": "npm --prefix frontend-nextjs run desktop:build"
  }
}
